spring:
  application:
    name: config-module
  profiles:
    active: test # comment it for dev purposes, uncomment for tests and pushing to master
  cloud.config.server.git.uri: file://${E_ARB_LOCAL_CFG_PATH}

